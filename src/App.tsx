import React, { ReactElement } from 'react';

const App = (): ReactElement => {
    return (
        <div data-testid="App">
            <h1>Typescript React App</h1>
        </div>
    );
};

export default App;
